/* Fill these in once you've created your Supabase project — see
   supabase/README.md for the 5-minute setup. Safe to commit: the anon
   key is meant to be public-facing, Row Level Security (defined in
   supabase/schema.sql) is what actually controls who can read/write
   what — not secrecy of this key. */
var SUPABASE_URL = "";       // e.g. "https://xxxxx.supabase.co"
var SUPABASE_ANON_KEY = "";  // the "anon" / "public" key from Settings -> API
