/* Event/flyer data for the BayPinned board - separate from PLACES (the
   map pins). An event can optionally link to a PLACES pin via linkedPin
   so its flyer and its map location share one spot instead of
   duplicating lat/lng. The board UI itself doesn't exist in this repo
   yet - this is scaffolding managed through admin.html so the data
   model is ready before that work starts. */
var EVENTS = [];
